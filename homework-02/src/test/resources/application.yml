spring:
  datasource:
    url: jdbc:h2:mem:testdb

  sql:
    init:
      mode: always
      data-locations: testdata.sql

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
      show-sql: true

#    schema-locations: schema.sql
#
#  h2:
#    console:
#    path: h2-console
#    settings:
#    web-allow-others: true